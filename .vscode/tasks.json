{
    "tasks": [
        {
            "label": "Make Main",
            "type": "shell",
            "group": "build",
            "command": "make ",
            "options": {
                "cwd": "${workspaceFolder}/src"
            },
            "problemMatcher": "$gcc"
        }
    ],
    "version": "2.0.0"
}